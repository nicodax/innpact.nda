= render partial: "settings/shared/breadcrumb", locals: {intermediate_topics: {t('activerecord.models.interest_rate_type.other') => fund_settings_interest_rate_types_path}, current_topic: setting.name}
= render partial: "settings/interest_rate_types/shared/header"
%hr
  .grid-x.grid-padding-x
    
    = render partial: "settings/interest_rate_types/shared/index"
    .cell.small-12.medium-4
      %h2= t("general.edit")
      = simple_form_for setting, url: fund_settings_interest_rate_type_path(id: setting.id) do |f|
        = render 'shared/form_error_messages', object: f.object
        = render partial: "settings/interest_rate_types/shared/form", locals: {f: f}