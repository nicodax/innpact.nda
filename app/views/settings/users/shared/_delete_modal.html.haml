#delete_user_modal.reveal{"data-reveal" => ""}
  %h1= t('.are_you_sure')
  - if user.im_groups.any?
    %h2= t('.delete_user_consequences_im_groups')
    %ul
      - user.im_groups.each do |im_group|
        %li= im_group.name
  = link_to t(".delete"), settings_user_path(id: user), class: "cta cta_warning ml5", method: :delete
  %button.cta.cta_button.cta_right{"aria-label" => "Close modal", "data-close" => "", :type => "button"}
    Close
  %button.close-button{"aria-label" => "Close modal", "data-close" => "", :type => "button"}
    %span{"aria-hidden" => "true"} ×
