= render partial: "settings/shared/breadcrumb", locals: {intermediate_topics: {t(".institutions") => fund_settings_institutions_path, @institution.name => fund_settings_institution_path(fund_id: @institution.fund.id, id: @institution.id), t("settings.financial.new.breadcrumb_financial") => fund_settings_institution_path(fund_id: @institution.fund.id, id: @institution.id, anchor: 'financials_tab')}, current_topic: t("settings.breadcrumb_edit")}
= render partial: "settings/institutions/shared/header", locals: {current_institution: @institution, subtitle: {t("settings.financial.new.breadcrumb_financial") => fund_settings_institution_path(fund_id: @institution.fund.id, id: @institution.id, anchor: 'financials_tab')}}

= simple_form_for(@institution_financial, method: :post, url: fund_settings_institution_institution_financial_path(fund_id: fund.id, institution_id: @institution.id)) do |f|
  = render partial: "settings/institution_financial/shared/form", locals: {f: f}

= javascript_pack_tag 'cleave_form_format'
