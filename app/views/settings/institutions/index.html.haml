= render partial: "settings/shared/breadcrumb", locals: {intermediate_topics: {}, current_topic: t(".institutions")}
= render partial: "settings/institutions/shared/header", locals: {current_institution: setting}
%hr
  .grid-x.grid-padding-x

    = render partial: "settings/institutions/shared/index"
    -if !current_user.reader?
      .cell.small-12.medium-10
        %h2=t("settings.shared.create")
        = simple_form_for new_setting, url: fund_settings_institutions_path do |f|
          = render partial: "settings/institutions/shared/simplified_form", locals: {f: f}
