- content_for :breadcrumbs do
  %nav{"aria-label" => "You are here:", :role => "navigation"}
    %ul.breadcrumbs
      %li
        %a{:href => root_path}= t('breacrumb.welcome')
      %li
        %a{:href => root_path}= t('breacrumb.funds')
      %li
        %a{:href => fund_path(id: fund.id)}= fund.name
      %li
        %span.show-for-sr Current:
        = t('breacrumb.critical_case')

  %h2= t('.critical_case') + fund.name

  = render partial: "critical_case_dashboards/shared/critical_cases_dashboard"
